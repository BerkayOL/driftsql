targets:
    $default:
      builders:
        drift_dev:
          # Default Drift generator test için kapatılıyor.
          enabled: false

        drift_dev:analyzer:
          enabled: true

        drift_dev:modular:
          enabled: true 